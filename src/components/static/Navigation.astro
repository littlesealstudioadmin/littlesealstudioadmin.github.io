---
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb";
import site_config from "@/site-config.json";

const { pathname } = Astro.props;
const isCurrent = (href: string) => pathname === href;
---

{
  site_config.bNavigation && (
    <>
      <Breadcrumb className="select-none">
        <BreadcrumbList>
          {/* Home */}
          <BreadcrumbItem>
            {isCurrent("/") ? (
              <BreadcrumbPage className="flex items-center">
                <img src="/favicon.svg" alt="리틀실스튜디오" class="h-6 w-auto inline-block" />
              </BreadcrumbPage>
            ) : (
              <BreadcrumbLink href="/" className="flex items-center hover:opacity-80 transition-opacity">
                <img src="/favicon.svg" alt="리틀실스튜디오" class="h-6 w-auto inline-block" />
              </BreadcrumbLink>
            )}
          </BreadcrumbItem>

          {site_config.navigationItems &&
            site_config.navigationItems.map((item) => (
              <>
                <BreadcrumbSeparator>•</BreadcrumbSeparator>
                <BreadcrumbItem>
                  {pathname.startsWith(`/${item}`) ? (
                    <BreadcrumbPage>{item}</BreadcrumbPage>
                  ) : (
                    <BreadcrumbLink href={`/${item}`}>{item}</BreadcrumbLink>
                  )}
                </BreadcrumbItem>
              </>
            ))}
        </BreadcrumbList>
      </Breadcrumb>
    </>
  )
}
